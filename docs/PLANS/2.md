# Stage 2 Implementation Plan: Traffic Monitor & PcapPlusPlus Integration

## Goal
Implement the core network packet capture functionality using PcapPlusPlus. This involves creating the `TrafficMonitor` class which will handle reading from PCAP files and live network interfaces.

## Proposed Changes

### Directory Structure
- Create `src/network/` directory.

### New Files

#### [NEW] [src/network/TrafficMonitor.hpp](file:///c:/Dev/CXX/diploma_test/src/network/TrafficMonitor.hpp)
- Class definition for `TrafficMonitor`.
- Methods: `openFile(std::string path)`, `startCapture(std::string interfaceName)`.
- Members: PcapPlusPlus device pointers/handles.

#### [NEW] [src/network/TrafficMonitor.cpp](file:///c:/Dev/CXX/diploma_test/src/network/TrafficMonitor.cpp)
- Implementation of `TrafficMonitor`.
- `openFile`: Opens a `PcapFileReaderDevice`.
- `startCapture`: Opens a `PcapLiveDevice` and starts capture with a callback.

### Modified Files

#### [MODIFY] [CMakeLists.txt](file:///c:/Dev/CXX/diploma_test/CMakeLists.txt)
- Add `src/network/TrafficMonitor.cpp` to executable sources.
- Ensure `include_directories` covers `src`.

#### [MODIFY] [src/main.cpp](file:///c:/Dev/CXX/diploma_test/src/main.cpp)
- Temporary code to instantiate `TrafficMonitor` and verify PcapPlusPlus linking.

## Verification Plan

### Automated Tests
- None yet (Project uses simple main integration test for now).

### Manual Verification
1. **Build**: Run CMake build in `build/` directory.
   ```powershell
   cd build
   cmake ..
   cmake --build .
   ```
2. **Run**:
   ```powershell
   .\Debug\DiplomDDoS.exe
   ```
3. **Observation**: Output should indicate successful PcapPlusPlus initialization or list available devices (if we add that check).
