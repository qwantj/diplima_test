# Final Integration (Stage 4)

## Goal
Connect all components (TrafficMonitor → FeatureExtractor → ModelInferencer) into a real-time detection pipeline with a proper GUI.

## Proposed Changes

### Fix Model Prediction

#### [MODIFY] [ModelInferencer.cpp](file:///c:/Dev/CXX/diploma_test/src/ml/ModelInferencer.cpp)
- Change output type from `long long*` to `int64_t*` for proper ONNX label reading
- Add debug logging for output tensor shape/type
- Add method to get probability (from `output_probability`)

---

### Detection Engine

#### [NEW] src/core/DetectionEngine.hpp
- Orchestrates: TrafficMonitor → FeatureExtractor → ModelInferencer
- Provides callbacks for UI updates
- Manages start/stop capture lifecycle

#### [NEW] src/core/DetectionEngine.cpp
- Implements real-time packet processing loop
- Aggregates packets into flows
- Calls inference on flow features
- Emits detection signals

---

### User Interface

#### [MODIFY] [main.cpp](file:///c:/Dev/CXX/diploma_test/src/main.cpp)
- Replace test code with proper `MainWindow`
- Add interface selector dropdown
- Add Start/Stop buttons
- Add status display showing:
  - Current interface
  - Packets analyzed
  - Attacks detected
  - Last prediction result

---

## Verification Plan

### Build Test
```bash
cmake --build build --config Release
```

### Manual Test
1. Run application as admin
2. Select network interface
3. Click Start
4. Generate network traffic
5. Verify predictions appear in UI

---

## Database Integration (Stage 5)

### Goal
Implement persistent storage for detected incidents using PostgreSQL.

### Database Schema
```sql
CREATE TABLE incidents (
    id SERIAL PRIMARY KEY,
    timestamp TIMESTAMP NOT NULL,
    model_used VARCHAR(50),
    confidence FLOAT,
    flow_key VARCHAR(100),
    is_attack BOOLEAN
);
```

### Proposed Changes

#### [NEW] src/db/DatabaseManager.hpp
- Singleton or managed class for DB connection
- Methods: `connect()`, `logIncident(result)`
- Uses `QSqlDatabase`

#### [MODIFY] [DetectionEngine.cpp](file:///c:/Dev/CXX/diploma_test/src/core/DetectionEngine.cpp)
- Integrate `DatabaseManager`
- Call `logIncident` when `result.label == 1`

#### [MODIFY] [MainWindow.cpp](file:///c:/Dev/CXX/diploma_test/src/main.cpp)
- Add "History" tab
- Use `QSqlTableModel` or `QTableView` to show incidents

### Verification Plan
1. Helper script to create DB/Table (or auto-create in app).
2. Generate attack traffic.
3. Verify rows in `incidents` table.

