- [ ] Обойти ограничение на 1 выход: в `ModelInferencer.cpp` и `.hpp` сохранять имена *всех* выходных нод модели.
- [ ] В `predict()` запрашивать у `Run()` все выходы модели.
- [ ] На лету проверять тип выходных данных (Tensor или Sequence of Maps):
  - [ ] **Если Tensor (Нейросети, CatBoost)**: для массива размера 2 брать значение под индексом `[1]`, для массива 1 - индекс `[0]`.
  - [ ] **Если Sequence of Maps (Scikit-Learn / skl2onnx)**: корректно извлекать `Map` из `Sequence` и доставать значение вероятности (Float) для класса `1` (Атака).
  - [ ] **Учет жестких моделей (без вероятностей)**: если модель возвращает классификацию без вероятностей вообще, отдавать `1.0` при метке 1 (Атака) и `0.0` при метке 0 (Норма).
- [ ] Добавить в алгоритм расчета (`FeatureExtractor.cpp`) счетчики TCP-флагов: SYN, ACK, FIN, RST, PSH, URG.
- [ ] Добавить сбор статистики по TCP Window Size.
- [ ] Сохранять все новые параметры в словарь `varMap` в функции `calculateRawFeatures()`.
- [ ] Адаптировать генератор документации (`getFeatureDocumentation`) для новых признаков.
