# Увеличение точности MLP

- [x] В `colab_train_code.py` увеличить размер сети (`hidden_layer_sizes=(128, 64, 32)`) и `max_iter`.
- [x] То же самое сделать в локальном `train.py`.
- [x] Опционально: увеличить размер батча или изменить solver, если сеть станет слишком долго обучаться (добавлено `early_stopping=True`).
